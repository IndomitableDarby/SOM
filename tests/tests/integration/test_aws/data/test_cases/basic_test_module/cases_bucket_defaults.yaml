- name: cloudtrail_defaults
  description: CloudTrail default configurations
  configuration_parameters:
    BUCKET_TYPE: cloudtrail
    BUCKET_NAME: som-cloudtrail-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: cloudtrail
    bucket_name: som-cloudtrail-integration-tests

- name: vpc_defaults
  description: VPC default configurations
  configuration_parameters:
    BUCKET_TYPE: vpcflow
    BUCKET_NAME: som-vpcflow-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: vpcflow
    bucket_name: som-vpcflow-integration-tests

- name: config_defaults
  description: Config default configurations
  configuration_parameters:
    BUCKET_TYPE: config
    BUCKET_NAME: som-config-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: config
    bucket_name: som-config-integration-tests

- name: alb_defaults
  description: ALB default configurations
  configuration_parameters:
    BUCKET_TYPE: alb
    BUCKET_NAME: som-alb-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: alb
    bucket_name: som-alb-integration-tests

- name: clb_defaults
  description: CLB default configurations
  configuration_parameters:
    BUCKET_TYPE: clb
    BUCKET_NAME: som-clb-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: clb
    bucket_name: som-clb-integration-tests

- name: nlb_defaults
  description: NLB default configurations
  configuration_parameters:
    BUCKET_TYPE: nlb
    BUCKET_NAME: som-nlb-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: nlb
    bucket_name: som-nlb-integration-tests

- name: kms_defaults
  description: KMS default configurations
  configuration_parameters:
    BUCKET_TYPE: custom
    BUCKET_NAME: som-kms-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: custom
    bucket_name: som-kms-integration-tests

- name: macie_defaults
  description: CloudTrail default configurations
  configuration_parameters:
    BUCKET_TYPE: custom
    BUCKET_NAME: som-macie-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: custom
    bucket_name: som-macie-integration-tests

- name: trusted_advisor_defaults
  description: Trusted Advisor default configurations
  configuration_parameters:
    BUCKET_TYPE: custom
    BUCKET_NAME: som-trusted-advisor-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: custom
    bucket_name: som-trusted-advisor-integration-tests

- name: guardduty_defaults
  description: GuardDuty default configurations
  configuration_parameters:
    BUCKET_TYPE: guardduty
    BUCKET_NAME: som-guardduty-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: guardduty
    bucket_name: som-guardduty-integration-tests

- name: native_guardduty_defaults
  description: Native GuardDuty default configurations
  configuration_parameters:
    BUCKET_TYPE: guardduty
    BUCKET_NAME: som-native-guardduty-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: guardduty
    bucket_name: som-native-guardduty-integration-tests

- name: waf_defaults
  description: WAF default configurations
  configuration_parameters:
    BUCKET_TYPE: waf
    BUCKET_NAME: som-waf-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: waf
    bucket_name: som-waf-integration-tests

- name: server_access_defaults
  description: Server Access default configurations
  configuration_parameters:
    BUCKET_TYPE: server_access
    BUCKET_NAME: som-server-access-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: server_access
    bucket_name: som-server-access-integration-tests

- name: cisco_umbrella_defaults
  description: Umbrella default configurations
  configuration_parameters:
    BUCKET_TYPE: cisco_umbrella
    BUCKET_NAME: som-umbrella-integration-tests
  metadata:
    resource_type: bucket
    bucket_type: cisco_umbrella
    bucket_name: som-umbrella-integration-tests
